# FastAPI and server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Database
# Capped below 2.1 until the upgrade is done on purpose: 2.1.0 reached CI on 2026-09-24 and
# broke it (plain postgresql:// URLs want psycopg 3, and new typing errors). H32 replaces
# these ranges with a lock file.
sqlalchemy>=2.0.25,<2.1
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
alembic>=1.13.1

# Async support
aiofiles>=23.2.1

# Redis and Celery
redis>=5.0.1

# Image processing (for thumbnails, not OCR)
pillow>=10.2.0

# PDF processing
pdfplumber>=0.11.0

# Fuzzy matching
rapidfuzz>=3.6.1

# HTTP client (for MinerU, vLLM, Open Food Facts)
httpx>=0.26.0

# Configuration
python-dotenv>=1.0.1
pydantic-settings>=2.3.0

# Logging
structlog>=24.1.0

# Date/time utilities
python-dateutil>=2.8.2

# Testing (dev dependencies)
pytest>=8.0.0
pytest-asyncio>=0.23.4
pytest-cov>=4.1.0
pytest-mock>=3.12.0
aiosqlite>=0.22.0

# Code quality (dev dependencies)
ruff==0.12.12
mypy>=1.8.0
