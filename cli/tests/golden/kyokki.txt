usage: kyokki [-h] [--url URL] [--token TOKEN] [--json] [--verbose]
              COMMAND ...

Read and change the Kyokki kitchen inventory over its HTTP API.
Built for agents: stable exit codes, and JSON whenever stdout is not a terminal.

options:
  -h, --help     show this help message and exit

connection and output:
  --url URL      Kyokki base URL, e.g. http://kyokki.lan:8000 (default:
                 $KYOKKI_URL)
  --token TOKEN  API token, sent as a Bearer header and never printed
                 (default: $KYOKKI_TOKEN)
  --json         print the API's JSON (the default when stdout is not a
                 terminal)
  --verbose      log each request and its status to stderr

commands:
  COMMAND
    doctor       check the connection and the token
    stock        list, add and consume stock
    product      resolve and teach product names
    category     the category ids for --category

examples:
  kyokki doctor
  kyokki stock list --expiring 3
  kyokki stock consume milk 2 dl --dry-run

environment:
  KYOKKI_URL    base URL of the Kyokki server (overridden by --url)
  KYOKKI_TOKEN  API token (overridden by --token); sent as a Bearer header only

exit codes:
  0 ok
  1 error: unreachable, server error (5xx) or an unexpected answer
  2 usage: bad arguments, or the API rejected the request (400 invalid, 422)
  3 not found
  4 ambiguous name: the candidates are printed on stdout
  5 insufficient stock
  6 conflict: the name belongs to another product, or a reused Idempotency-Key
  7 auth: missing or unknown token (401), or a read token on a write (403)
