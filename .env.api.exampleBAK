# --- API Environment ---
# The API connects to other services using their service names on the Docker network.

# PostgreSQL
POSTGRES_SERVER=postgres
POSTGRES_USER=fridge_user
POSTGRES_PASSWORD=supersecretpassword
POSTGRES_DB=fridge_logger

# Redis
REDIS_HOST=redis
REDIS_PORT=6379

# Ollama (not used by API, but required by config)
OLLAMA_HOST=http://placeholder
